# 多用户认证系统使用指南

## ✅ 已实现功能

### 1. 用户登录/注册
- ✅ 登录页面
- ✅ 用户注册功能
- ✅ 首次使用自动创建管理员账户
- ✅ 密码加密存储（bcrypt）

### 2. 会话管理
- ✅ 登录状态检查
- ✅ 用户信息显示（右上角）
- ✅ 登出功能
- ✅ 登录日志记录

### 3. 用户管理（管理员）
- ✅ 用户列表查看
- ✅ 搜索和筛选用户
- ✅ 创建新用户
- ✅ 删除用户
- ✅ 角色显示

### 4. 权限控制
- ✅ 基于角色的访问控制（RBAC）
- ✅ 菜单权限控制（不同角色看到不同菜单）
- ✅ 页面权限检查
- ✅ 数据隔离（用户只能看到自己的数据）

### 5. 数据隔离
- ✅ 课程列表按用户角色过滤
- ✅ 首页统计按用户角色显示
- ✅ 课程管理权限检查

## 角色说明

### 超级管理员 (super_admin)
- 所有权限
- 可以管理所有用户
- 可以查看所有数据
- 可以查看操作日志

### 学校管理员 (school_admin)
- 可以管理用户（部分）
- 可以创建课程
- 可以查看所有数据
- 可以查看操作日志

### 教师 (teacher)
- 可以创建和管理自己的课程
- 可以创建任务和录入成绩
- 只能看到自己的数据
- 可以查看操作日志

### 助教 (ta)
- 可以协助管理被授权的课程
- 可以录入成绩
- 只能看到被授权课程的数据

### 学生 (student)
- 只能查看自己的成绩
- 无法创建课程或任务

## 首次使用

### 创建管理员账户
1. 启动应用：`streamlit run app.py`
2. 在登录页面点击"首次使用（创建管理员账户）"
3. 输入管理员用户名和密码（默认：admin / admin123）
4. 点击"创建管理员"
5. 使用创建的管理员账户登录

### 注册新用户
1. 在登录页面点击"注册"
2. 填写用户信息
3. 选择角色（默认：teacher）
4. 提交注册

## 使用流程

### 管理员
1. 登录系统
2. 在"用户管理"中创建新用户
3. 为不同用户分配不同角色
4. 查看"操作日志"了解系统使用情况

### 教师
1. 登录系统
2. 在"课程管理"中创建课程
3. 在"任务管理"中创建作业/考试
4. 在"成绩录入"中录入学生成绩

### 助教
1. 等待教师授权课程
2. 登录系统查看被授权的课程
3. 协助录入成绩

## 安全特性

1. **密码加密**：使用bcrypt加密存储密码
2. **权限验证**：每次操作都检查用户权限
3. **数据隔离**：用户只能访问被授权的数据
4. **操作日志**：记录所有重要操作，便于审计
5. **会话管理**：登录状态保存在session state中

## 注意事项

1. **默认管理员**：首次使用务必创建管理员账户
2. **密码安全**：建议修改默认密码（admin123）
3. **角色权限**：合理分配用户角色，避免权限过大
4. **数据备份**：定期备份数据库文件
5. **日志清理**：定期清理旧日志，避免数据库过大

## 常见问题

### Q: 忘记了管理员密码怎么办？
A: 删除数据库文件重新创建，或通过数据库直接修改password_hash字段（不推荐）

### Q: 如何批量导入用户？
A: 目前需要通过"用户管理"界面逐个创建，或直接操作数据库（需要数据库知识）

### Q: 如何修改用户角色？
A: 目前需要通过数据库直接修改，或删除用户后重新创建（后续可以添加编辑功能）

### Q: 如何恢复被删除的用户？
A: 如果数据库有备份，可以恢复。否则需要重新创建用户。

## 后续改进建议

- [ ] 用户编辑功能（修改角色、邮箱等）
- [ ] 密码重置功能
- [ ] 批量导入用户（Excel）
- [ ] 用户激活/禁用功能
- [ ] 更细粒度的权限控制
- [ ] 课程授权管理界面

